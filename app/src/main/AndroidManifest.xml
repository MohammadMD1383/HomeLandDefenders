<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="ir.androidDev.homeLandDefenders">
	
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	
	<application
		android:name=".contentManagement.PicassoHandler"
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher"
		android:supportsRtl="true"
		android:theme="@style/AppTheme"
		android:usesCleartextTraffic="true">
		<activity android:name=".WarHistoryActivity" />
		<activity android:name=".pages.OperationPageActivity" />
		<activity android:name=".pages.TestamentPageActivity" />
		<activity android:name=".pages.BiographyPageActivity" />
		<activity android:name=".VeteransActivity" />
		<activity android:name=".OperationActivity" />
		<activity android:name=".TestamentActivity" />
		<activity android:name=".BiographyActivity" />
		<activity android:name=".IntroductionActivity" />
		<activity
			android:name=".HomePageActivity"
			android:launchMode="singleTask" />
		<activity
			android:name=".MainActivity"
			android:theme="@style/MainActivity"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		
		<uses-library
			android:name="org.apache.http.legacy"
			android:required="false" />
	</application>

</manifest>